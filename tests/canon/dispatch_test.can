Dark = Unit

DispatchSelectsDarkArm = TestResult

DispatchSelectsLightArm = TestResult

Light = Unit

Unit => DispatchSelectsDarkArm {
    Dark() -> (
        * Dark { Pass() }
        * Light { Fail() }
    )
}

Mode = Dark + Light

Unit => DispatchSelectsLightArm {
    Light() -> (
        * Dark { Fail() }
        * Light { Pass() }
    )
}
