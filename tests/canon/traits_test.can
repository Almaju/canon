Greeting = String

Name = String

Show = () => String

Show = (Greeting) => String {
    "HELLO!"
}

Show = (Name) => String {
    "Alice"
}

testShowExists = () => TestResult {
    "hi"
        -> Greeting
        -> Show
    "Alice"
        -> Name
        -> Show
    Pass()
}
