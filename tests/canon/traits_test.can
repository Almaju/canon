Greeting = String

Name = String

Show = () => String

Show = (Greeting) => String {
    "HELLO!"
}

Show = (Name) => String {
    "Alice"
}

testShowExists = () => TestResult {
    Greeting("hi") -> Show
    Name("Alice") -> Show
    Pass()
}
