Greeting = String

Name = String

Show = () => String

Show = (Greeting) => String {
    "HELLO!"
}

Show = (Name) => String {
    "Alice"
}

ShowResolvesByInputType = () => TestResult {
    "hi"
        -> Greeting
        -> Show
    "Alice"
        -> Name
        -> Show
    Pass()
}
