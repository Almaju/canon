Greeting = String

Name = String

Show = () => String

Show = (Greeting) => String {
    "HELLO!"
}

Show = (Name) => String {
    "Alice"
}

ShowResolvesByInputType = TestResult

Unit => ShowResolvesByInputType {
    Greeting("hi") -> Show
    Name("Alice") -> Show
    Pass()
}
