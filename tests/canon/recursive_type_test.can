Branch = Left * Right * Value

DispatchSelectsLeafArm = TestResult

Leaf = Unit

Left = Tree

Right = Tree

Tree = Branch + Leaf

Value = Int

Unit => DispatchSelectsLeafArm {
    Leaf() -> (
        * Branch => TestResult { Fail() }
        * Leaf => TestResult { Pass() }
    )
}
