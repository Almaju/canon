main = () => Unit {
    List("\"a\"", "\"b\"", "\"c\"")
        -> Json
        -> Print
    List("1", "2", "3")
        -> Json
        -> Print
    List("\"only\"")
        -> Json
        -> Print
}
