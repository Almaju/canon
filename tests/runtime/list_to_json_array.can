use canon/std/Json

main = () -> Unit {
    List("\"a\"", "\"b\"", "\"c\"")
        .Json()
        .print()
    List("1", "2", "3")
        .Json()
        .print()
    List("\"only\"")
        .Json()
        .print()
}
