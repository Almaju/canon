Chain = Link + Stop

Len = Int

Link = Next

Next = Chain

Chain => Len {
    Chain -> (
        * Link { Link.Next -> Len -> Sum(1) }
        * Stop { 0 }
    )
}

Unit => Program {
    Stop()
        -> Next
        -> Link
        -> Next
        -> Link
        -> Len
        -> Print
}
