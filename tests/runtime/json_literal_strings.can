greeting = (String) => Json {
    {"hello":String,"len":5}
}

Unit => Program {
    "world"
        .greeting()
        -> Print
    {"escaped":"a \"b\" c"} -> Print
    {"control":"tab:\there"} -> Print
}
