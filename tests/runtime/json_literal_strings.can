Greeting = Json

String => Greeting {
    {"hello":String,"len":5}
}

Unit => Program {
    "world"
        -> Greeting
        -> Print
    {"escaped":"a \"b\" c"} -> Print
    {"control":"tab:\there"} -> Print
}
