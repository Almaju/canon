greeting = (String) => Json {
    {"hello":String,"len":5}
}

main = () => Unit {
    "world"
        .greeting()
        -> Print
    {"escaped":"a \"b\" c"} -> Print
    {"control":"tab:\there"} -> Print
}
