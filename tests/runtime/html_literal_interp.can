row = (String) => Html {
    <li>{String}</li>
}

tag = (Msg) => Html {
    <button data-msg="{Msg}">go</button>
}

main = () => Unit {
    <ul>{"a & b".row()}{"x<y".row()}</ul> -> Print
    <span>{1 -> Sum(41)}</span> -> Print
    Msg("inc")
        .tag()
        -> Print
    <p>5 &lt; 6 &amp; 7</p> -> Print
}
