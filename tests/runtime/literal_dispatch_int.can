Described = (Int) => Unit {
    Int -> (
        * 0 => Unit { "zero" -> Print }
        * 1 => Unit { "one" -> Print }
        * Int => Unit { Int -> Print }
    )
}

Unit => Program {
    0 -> Described
    1 -> Described
    42 -> Described
}
