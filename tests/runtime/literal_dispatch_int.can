describe = (Int) => Unit {
    Int.(
        * (0) => Unit { "zero" -> Print }
        * (1) => Unit { "one" -> Print }
        * (Int) => Unit { Int -> Print }
    )
}

main = () => Unit {
    0.describe()
    1.describe()
    42.describe()
}
