Described = Unit

Int => Described {
    Int -> (
        * 0 { "zero" -> Print }
        * 1 { "one" -> Print }
        * Int { Int -> Print }
    )
}

Unit => Program {
    Described(0)
    Described(1)
    Described(42)
}
