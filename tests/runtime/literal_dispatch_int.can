Described = Unit

Int => Described {
    Int -> (
        * 0 => Described { "zero" -> Print }
        * 1 => Described { "one" -> Print }
        * Int => Described { Int -> Print }
    )
}

Unit => Program {
    Described(0)
    Described(1)
    Described(42)
}
