half = (Bool) => Float {
    Bool -> (
        * False => Float { 1.5 }
        * True => Float { 2.5 }
    )
}

main = () => Unit {
    True()
        .half()
        -> Print
    False()
        .half()
        -> Print
}
