main = () => Result<Unit, MalformedJson> {
    Json("[1, 2, 3]")? -> Print
    Json("{\"a\":1,\"b\":[true,false,null]}")? -> Print
    Json("\"a plain string\"")? -> Print
    Json("42")? -> Print
    Json("null")? -> Print
    Ok(Unit())
}
