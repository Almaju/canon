Doc = String

Unit => Result<Program, MalformedJson> {
    Doc("{\"model\":\"gpt-4\",\"stream\":true}")
        -> Json?
        -> Field("model")?
        -> Decoded?
        -> Print
    Unit() -> Ok
}
