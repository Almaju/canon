use canon/std/Json
use canon/std/MalformedJson

main = () -> Result<Unit, MalformedJson> {
    Json("{\"model\":\"gpt-4\",\"stream\":true}")?
        .field("model")?
        .asString()?
        .print()
    Ok(Unit())
}
