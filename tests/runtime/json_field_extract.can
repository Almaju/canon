Unit => Result<Program, MalformedJson> {
    "{\"model\":\"gpt-4\","
        -> Joined("\"stream\":true}")
        -> Json?
        -> Field("model")?
        -> Decoded?
        -> Print
    Unit() -> Ok
}
