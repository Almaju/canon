main = () => Unit {
    Args()
        .length()
        .print()
    Args()
        .Json()
        .print()
    Cwd().(
        * (None) => Unit { "no cwd".print() }
        * (Some<String>) => Unit { "cwd: ".concat(String).print() }
    )
}
