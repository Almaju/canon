Unit => Program {
    Args()
        -> Length
        -> Print
    Args()
        -> Json
        -> Print
    Cwd() -> (
        * None { "no cwd" -> Print }
        * Some<String> { `cwd: {String}` -> Print }
    )
}
