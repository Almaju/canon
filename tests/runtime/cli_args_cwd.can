Unit => Program {
    Args()
        -> Length
        -> Print
    Args()
        -> Json
        -> Print
    Cwd() -> (
        * None => Unit { "no cwd" -> Print }
        * Some<String> => Unit { "cwd: " -> Joined(String) -> Print }
    )
}
