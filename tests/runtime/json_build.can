Unit => Result<Program, MalformedJson> {
    "{\"answer\":"
        -> Joined(Encoded(42))
        -> Joined(",\"label\":")
        -> Joined(Encoded("life"))
        -> Joined("}")
        -> Json?
        -> Print
    Unit() -> Ok
}
