main = () => Result<Unit, MalformedJson> {
    "{\"answer\":"
        -> Joined(42 -> ToJson)
        -> Joined(",\"label\":")
        -> Joined("life" -> ToJson)
        -> Joined("}")
        -> Json?
        -> Print
    Unit() -> Ok
}
