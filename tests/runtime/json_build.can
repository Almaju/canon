Unit => Result<Program, MalformedJson> {
    "{\"answer\":"
        -> Joined(ToJson(42))
        -> Joined(",\"label\":")
        -> Joined(ToJson("life"))
        -> Joined("}")
        -> Json?
        -> Print
    Unit() -> Ok
}
