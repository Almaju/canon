First = (Int) => Option<String> {
    Int -> Eq(0) -> (
        * False => Option<String> { "found" -> Some }
        * True => Option<String> { None() }
    )
}

Second = (Int) => Option<String> {
    Int
        -> First?
        -> Joined(" and used")
        -> Some
}

Unit => Program {
    5 -> Second -> (
        * None => Unit { "none" -> Print }
        * Some<String> => Unit { String -> Print }
    )
    0 -> Second -> (
        * None => Unit { "none" -> Print }
        * Some<String> => Unit { String -> Print }
    )
}
