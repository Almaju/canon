Unit => Result<Program, Unit> {
    Ok("model") -> (
        * Err<Unit> => Unit { "err" -> Print }
        * Ok<String> => Unit {
            "first: literal" -> Print
            String -> Print
            `echo: {String}:{String}` -> Print
        }
    )
    Unit() -> Ok
}
