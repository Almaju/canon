use oneway/std/Json

greeting = (String) -> Json {
    {"hello":String,"len":5}
}

main = () -> Unit {
    "world"
        .greeting()
        .print()
    {"escaped":"a \"b\" c"}.print()
    {"control":"tab:\there"}.print()
}
