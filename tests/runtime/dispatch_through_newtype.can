Content = String

MessageContent = Option<Content>

dispatch = (MessageContent) => Unit {
    MessageContent -> (
        * None => Unit { "absent" -> Print }
        * Some<Content> => Unit { "present" -> Print }
    )
}

main = () => Unit {
    MessageContent(Some("hi")).dispatch()
    MessageContent(None()).dispatch()
}
