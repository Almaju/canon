Content = String

Dispatched = Unit

MessageContent = Option<Content>

MessageContent => Dispatched {
    MessageContent -> (
        * None { "absent" -> Print }
        * Some<Content> { "present" -> Print }
    )
}

Unit => Program {
    Some("hi")
        -> MessageContent
        -> Dispatched
    None()
        -> MessageContent
        -> Dispatched
}
