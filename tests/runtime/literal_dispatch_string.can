Routed = (String) => String {
    String -> (
        * "/notes" => String { "index" }
        * "/notes/1" => String { "note one" }
        * String => String { "not found: " -> Joined(String) }
    )
}

Unit => Program {
    "/notes"
        -> Routed
        -> Print
    "/notes/1"
        -> Routed
        -> Print
    "/oops"
        -> Routed
        -> Print
}
