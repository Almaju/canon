Routed = String

String => Routed {
    String -> (
        * "/notes" { "index" }
        * "/notes/1" { "note one" }
        * String { `not found: {String}` }
    )
}

Unit => Program {
    Routed("/notes") -> Print
    Routed("/notes/1") -> Print
    Routed("/oops") -> Print
}
