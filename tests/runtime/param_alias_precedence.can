Attr = String

Tag = String

Wrapped = String

Attr * String * Tag => Wrapped {
    `<{Tag} {Attr}>{String}</{Tag}>`
}

Unit => Program {
    Attr("class=\"x\"")
        -> Wrapped("inner" * Tag("div"))
        -> Print
}
