Attr = String

Tag = String

Wrapped = String

Attr * String * Tag => Wrapped {
    "<"
        -> Joined(Tag)
        -> Joined(" ")
        -> Joined(Attr)
        -> Joined(">")
        -> Joined(String)
        -> Joined("</")
        -> Joined(Tag)
        -> Joined(">")
}

Unit => Program {
    "class=\"x\""
        -> Attr
        -> Wrapped("inner" * "div" -> Tag)
        -> Print
}
