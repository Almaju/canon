Count = Int

Empty = Unit

MadeCount = Shape

MadeEmpty = Shape

MadeName = Shape

MadeRatio = Shape

Name = String

Ratio = Float

Shape = Count
  + Empty
  + Name
  + Ratio

Shown = Unit

Unit => MadeCount {
    Count(42)
}

Unit => MadeEmpty {
    Empty()
}

Unit => MadeName {
    Name("alex")
}

Unit => MadeRatio {
    Ratio(2.5)
}

Shape => Shown {
    Shape -> (
        * Count { Count -> Print }
        * Empty { "empty" -> Print }
        * Name { Name -> Print }
        * Ratio { Ratio -> Print }
    )
}

Unit => Program {
    MadeCount() -> Shown
    MadeEmpty() -> Shown
    MadeName() -> Shown
    MadeRatio() -> Shown
}
