ChatCompletionRequest = Model

Model = String

Json => Result<ChatCompletionRequest, MalformedJson> {
    Json
        -> Field("model")?
        -> Decoded?
        -> Model
        -> Ok
}

Unit => Result<Program, MalformedJson> {
    Json("{\"model\":\"gpt-4\"}")?
        -> ChatCompletionRequest?
        -> Print
    Unit() -> Ok
}
