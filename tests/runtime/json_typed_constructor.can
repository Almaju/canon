ChatCompletionRequest = Model

Model = String

ChatCompletionRequest = (Json) => Result<ChatCompletionRequest, MalformedJson> {
    Json
        .field("model")?
        .asString()
}

main = () => Result<Unit, MalformedJson> {
    "{\"model\":\"gpt-4\"}"
        -> Json?
        -> ChatCompletionRequest?
        -> Print
    Unit() -> Ok
}
