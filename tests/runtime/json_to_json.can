Unit => Program {
    Encoded(42) -> Print
    Encoded("hello") -> Print
    Encoded(1.5) -> Print
    True()
        -> Encoded
        -> Print
    False()
        -> Encoded
        -> Print
    Encoded("with \"quotes\" and \nnewline") -> Print
}
