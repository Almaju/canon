Unit => Program {
    Int("5") -> (
        * Err<MalformedInt> => Unit { "bad" -> Print }
        * Ok<Int> => Unit { "parsed" -> Print }
    )
    "a" -> Ge("b") -> (
        * False { "lt" -> Print }
        * True { "ge" -> Print }
    )
}
