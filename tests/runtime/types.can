Birthday = String

Bit = One + Zero

Ord = Equal
  + Greater
  + Less

OtherUser = User

User = Birthday * Username

Username = String

Unit => Program {
    One()
    Equal()
    Birthday("1990")
        -> User(Username("canon"))
        -> OtherUser
    "type definitions parsed" -> Print
}
