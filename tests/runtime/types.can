Birthday = String

Bit = One + Zero

Byte = Bit^8

Bytes = Byte^*

Ord = Equal
  + Greater
  + Less

OtherUser = User

User = Birthday * Username

Username = String

Unit => Program {
    "type definitions parsed" -> Print
}
