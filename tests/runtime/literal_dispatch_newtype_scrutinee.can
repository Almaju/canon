Prefix = String

Routed = Int

Int * String => Routed {
    String -> Substring(From(1) * To(4)) -> Prefix -> (
        * "Add:" { Int -> Sum(String -> Length) }
        * Prefix { Int }
    )
}

Unit => Program {
    10
        -> Routed("Add:xyz")
        -> Print
    10
        -> Routed("Nope")
        -> Print
}
