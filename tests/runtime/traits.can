Greeting = String

Name = String

Show = () => String

Show = (Greeting) => String {
    "HELLO!"
}

Show = (Name) => String {
    "Alice"
}

Unit => Program {
    Greeting("hi")
        -> Show
        -> Print
    Name("Alice")
        -> Show
        -> Print
}
