Greeting = String

Name = String

Show = () => String

Show = (Greeting) => String {
    "HELLO!"
}

Show = (Name) => String {
    "Alice"
}

Unit => Program {
    "hi"
        -> Greeting
        -> Show
        -> Print
    "Alice"
        -> Name
        -> Show
        -> Print
}
