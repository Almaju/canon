Greeting = String

Name = String

greet = (Greeting * Name) => Greeting {
    Greeting
}

Unit => Program {
    "hello"
        -> Greeting
        .greet("world" -> Name)
    "world"
        -> Name
        .greet("hello" -> Greeting)
    "commutative calling works" -> Print
}
