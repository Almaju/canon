Greeting = String

Name = String

greet = (Greeting * Name) => Greeting {
    Greeting
}

main = () => Unit {
    "hello"
        -> Greeting
        .greet("world" -> Name)
    "world"
        -> Name
        .greet("hello" -> Greeting)
    "commutative calling works" -> Print
}
