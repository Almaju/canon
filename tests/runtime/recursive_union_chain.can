Chain = Link + Stop

Label = String

LastLabel = String

Length = Int

Link = Label * Next

Next = Chain

Chain => LastLabel {
    Chain -> (
        * Link {
            Link.Next -> Length -> Eq(0) -> (
                * False { Link.Next -> LastLabel }
                * True { Link.Label }
            )
        }
        * Stop { "" }
    )
}

Chain => Length {
    Chain -> (
        * Link { Link.Next -> Length -> Sum(1) }
        * Stop { 0 }
    )
}

Unit => Program {
    "a"
        -> Link("b" -> Link("c" -> Link(Stop())))
        -> Length
        -> Print
    "a"
        -> Link("b" -> Link(Stop()))
        -> LastLabel
        -> Print
}
