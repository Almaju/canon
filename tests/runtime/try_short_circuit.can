Classified = String

Described = String

Int => Result<Classified, String> {
    Int -> Eq(0) -> (
        * False { Ok("nonzero") }
        * True { Err("was zero") }
    )
}

Int => Result<Described, String> {
    Ok(`{Int -> Classified?} indeed`)
}

Unit => Program {
    Described(3) -> (
        * Err<String> => Unit { String -> Print }
        * Ok<String> => Unit { String -> Print }
    )
    Described(0) -> (
        * Err<String> { String -> Print }
        * Ok<String> { String -> Print }
    )
}
