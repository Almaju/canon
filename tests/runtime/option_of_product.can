Age = Int

Found = Option<User>

Name = String

User = Age * Name

Unit => Found {
    Age(7)
        -> User(Name("zoe"))
        -> Some
}

Unit => Program {
    Found() -> (
        * None { "none" -> Print }
        * Some<User> { User.Name -> Print }
    )
}
