Picked = String

Int => Option<Picked> {
    Int -> Eq(0) -> (
        * False => Option<Picked> { "payload" -> Some }
        * True => Option<Picked> { None() }
    )
}

Unit => Program {
    1 -> Picked -> (
        * None => Unit { "none" -> Print }
        * Some<String> => Unit { String -> Print }
    )
    0 -> Picked -> (
        * None => Unit { "none" -> Print }
        * Some<String> => Unit { String -> Print }
    )
}
