Unit => Result<Program, Unit> {
    42
        -> Ok?
        -> Print
    7 -> Some -> (
        * None => Unit { "absent" -> Print }
        * Some<Int> => Unit { "present" -> Print }
    )
    Unit() -> Ok
}
