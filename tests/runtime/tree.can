Branch = Left * Right * Value

Left = Tree

Right = Tree

Tree = Branch + Leaf

Value = Int

Described = (Tree) => String {
    Tree -> (
        * Branch => String { "branch" }
        * Leaf => String { "leaf" }
    )
}

Unit => Program {
    Leaf()
        -> Described
        -> Print
    7
        -> Value
        -> Branch(Leaf() -> Left * Leaf() -> Right)
        -> Described
        -> Print
}
