use oneway/std/Json
use oneway/std/MalformedJson

main = () -> Result<Unit, MalformedJson> {
    "{\"answer\":"
        .concat(42.ToJson())
        .concat(",\"label\":")
        .concat("life".ToJson())
        .concat("}")
        .Json()?
        .print()
    Ok(Unit())
}
