Birthday = String

User = Birthday * Username

Username = String

Unit => Program {
    Birthday("1990") -> User(Username("alice")).Username -> Print
    Birthday("1990") -> User(Username("alice")).Birthday -> Print
}
