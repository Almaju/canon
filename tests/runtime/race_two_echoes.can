use canon/std/concurrent

bindings "canon:builtins/http-server@0.1.0"

slowEcho = (String) -> Future<String>

main = () -> Unit {
    "first"
        .slowEcho()
        .race("second".slowEcho())
        .print()
}
