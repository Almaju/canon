Bag<T> = One<T>

One<T> = T

Shown<T> = String

<T: Ord>(Bag<T>) => Shown<T> {
    "a" -> Ord("b") -> (
        * Equal { "eq" }
        * Greater { "gt" }
        * Less { "lt" }
    )
}

Unit => Program {
    One(1.0)
        -> Bag
        -> Shown
        -> Print
}
