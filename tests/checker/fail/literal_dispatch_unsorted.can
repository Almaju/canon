Routed = String

String => Routed {
    String -> (
        * "/b" => Routed { "b" }
        * "/a" => Routed { "a" }
        * String => Routed { "other" }
    )
}

Unit => Program {
    "/a" -> Routed -> Print
}
