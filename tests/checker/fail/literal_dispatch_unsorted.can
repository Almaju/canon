route = (String) -> String {
    String.(
        * ("/b") -> String { "b" }
        * ("/a") -> String { "a" }
        * (String) -> String { "other" }
    )
}

main = () -> Unit {
    "/a".route().print()
}
