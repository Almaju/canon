route = (String) -> String {
    String.(
        * ("/a") -> String { "a" }
        * ("/a") -> String { "again" }
        * (String) -> String { "other" }
    )
}

main = () -> Unit {
    "/a".route().print()
}
