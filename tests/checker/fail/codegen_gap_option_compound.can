Age = Int

Found = Option<User>

Name = String

User = Age * Name

Unit => Found {
    User(Age(7) * Name("zoe")) -> Some
}

Unit => Program {
    Found() -> (
        * None { "none" -> Print }
        * Some<User> { User -> Name -> Print }
    )
}
