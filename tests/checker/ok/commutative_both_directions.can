Greeting = String

Name = String

Greeted = (Greeting * Name) => Greeting {
    Greeting
}

Unit => Program {
    "hello"
        -> Greeting
        -> Greeted("world" -> Name)
    "world"
        -> Name
        -> Greeted("hello" -> Greeting)
    "ok" -> Print
}
