Described = Unit

Extracted = String

JsonEntry = String

JsonMissing = String

JsonObject = JsonEntry

JsonValue = JsonMissing + JsonObject

JsonValue => Described {
    JsonValue -> (
        * JsonMissing { "missing" -> Print }
        * JsonObject { JsonObject -> Extracted -> Print }
    )
}

JsonObject => Extracted {
    "extracted"
}

Unit => Program {
    JsonEntry("payload")
        -> JsonObject
        -> Described
    "ok" -> Print
}
