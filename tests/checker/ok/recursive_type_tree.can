Branch = Left * Right * Value

Leaf = Unit

Left = Tree

Right = Tree

Tree = Branch + Leaf

Value = Int

Described = (Tree) => String {
    Tree -> (
        * Branch => String { "branch" }
        * Leaf => String { "leaf" }
    )
}

Unit => Program {
    Leaf()
        -> Described
        -> Print
}
