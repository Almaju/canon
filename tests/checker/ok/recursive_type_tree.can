Branch = Left * Right * Value

Described = String

Leaf = Unit

Left = Tree

Right = Tree

Tree = Branch + Leaf

Value = Int

Tree => Described {
    Tree -> (
        * Branch { "branch" }
        * Leaf { "leaf" }
    )
}

Unit => Program {
    Leaf()
        -> Described
        -> Print
}
