Content = String

Described = String

MessageContent = Option<Content>

MessageContent => Described {
    MessageContent -> (
        * None => Described { "absent" }
        * Some<Content> => Described { "present" }
    )
}

Unit => Program {
    "hi"
        -> Some
        -> MessageContent
        -> Described
        -> Print
}
