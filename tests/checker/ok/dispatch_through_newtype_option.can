Content = String

Described = String

MessageContent = Option<Content>

MessageContent => Described {
    MessageContent -> (
        * None { "absent" }
        * Some<Content> { "present" }
    )
}

Unit => Program {
    Some("hi")
        -> MessageContent
        -> Described
        -> Print
}
