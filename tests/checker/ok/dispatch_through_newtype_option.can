Content = String

MessageContent = Option<Content>

Described = (MessageContent) => String {
    MessageContent -> (
        * None => String { "absent" }
        * Some<Content> => String { "present" }
    )
}

Unit => Program {
    "hi"
        -> Some
        -> MessageContent
        -> Described
        -> Print
}
