Content = String

Described = String

Option => Described {
    Option -> (
        * None => Described { "none" }
        * Some<Content> => Described { "some" }
    )
}

Unit => Program {
    Content("hi")
        -> Some
        -> Described
        -> Print
}
