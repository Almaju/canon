Content = String

Described = String

Option => Described {
    Option -> (
        * None { "none" }
        * Some<Content> { "some" }
    )
}

Unit => Program {
    Content("hi")
        -> Some
        -> Described
        -> Print
}
