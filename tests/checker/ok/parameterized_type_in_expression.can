Content = String

Described = String

Option => Described {
    Option -> (
        * None => Described { "none" }
        * Some<Content> => Described { "some" }
    )
}

Unit => Program {
    "hi"
        -> Content
        -> Some
        -> Described
        -> Print
}
