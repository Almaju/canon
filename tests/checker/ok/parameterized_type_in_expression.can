Content = String

Described = (Option) => String {
    Option -> (
        * None => String { "none" }
        * Some<Content> => String { "some" }
    )
}

Unit => Program {
    "hi"
        -> Content
        -> Some
        -> Described
        -> Print
}
