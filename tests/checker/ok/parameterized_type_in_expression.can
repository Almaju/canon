Content = String

describeParam = (Option) => String {
    Option.(
        * (None) => String { "none" }
        * (Some<Content>) => String { "some" }
    )
}

main = () => Unit {
    "ok" -> Print
}
