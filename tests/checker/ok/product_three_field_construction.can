CompletionTokens = Int

PromptTokens = Int

TotalTokens = Int

Usage = CompletionTokens * PromptTokens * TotalTokens

main = () => Unit {
    Usage(CompletionTokens(0) * PromptTokens(0) * TotalTokens(0)).CompletionTokens.print()
}
