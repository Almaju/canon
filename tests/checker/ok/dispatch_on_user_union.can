Described = String

Equal = Unit

Greater = Unit

Less = Unit

Ord = Equal
  + Greater
  + Less

Ord => Described {
    Ord -> (
        * Equal { "equal" }
        * Greater { "greater" }
        * Less { "less" }
    )
}

Unit => Program {
    Equal()
        -> Described
        -> Print
}
