Closed = Unit

Mode = Closed + Opened

Opened = Unit

Described = (Mode) => String {
    Mode -> (
        * Closed => String { "closed" }
        * Opened => String { "opened" }
    )
}

Unit => Program {
    Closed()
        -> Described
        -> Print
}
