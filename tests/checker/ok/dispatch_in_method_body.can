Closed = Unit

Mode = Closed + Opened

Opened = Unit

describe = (Mode) => String {
    Mode -> (
        * Closed => String { "closed" }
        * Opened => String { "opened" }
    )
}

main = () => Unit {
    Closed()
        .describe()
        -> Print
}
