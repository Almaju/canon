Closed = Unit

Described = String

Mode = Closed + Opened

Opened = Unit

Mode => Described {
    Mode -> (
        * Closed { "closed" }
        * Opened { "opened" }
    )
}

Unit => Program {
    Closed()
        -> Described
        -> Print
}
