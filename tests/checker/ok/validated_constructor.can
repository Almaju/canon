InvalidUrl = String

Url = String

String => Result<Url, InvalidUrl> {
    String
        -> Url
        -> Ok
}

Unit => Program {
    Url("https://example.com")
    "validated ctor declared" -> Print
}
