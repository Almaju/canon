InvalidUrl = String

Url = String

Url = (String) => Result<Url, InvalidUrl> {
    String
        -> Url
        -> Ok
}

Unit => Program {
    "https://example.com" -> Url
    "validated ctor declared" -> Print
}
