main = () => Unit {
    List("a", "b", "c")
        .streamOf()
        -> Mapped((String) => String { String -> Joined("!") })
        .filter((String) => Bool { True() })
        .take(2)
        -> Joined(List("d").streamOf())
        .toString()
        -> Print
}
