#!/usr/bin/env sh
#
# pre-commit hook for canon
#
# Runs before every commit to make sure:
#   1. cargo test passes
#   2. all examples compile and run successfully (via the embedded
#      wasmtime — `canon run` produces no native binary)
#
# Install with:  just install-hooks
# Bypass once:   git commit --no-verify

set -e

echo "=== pre-commit: cargo fmt --check ==="
cargo fmt --check
echo "✓ formatting clean"

echo ""
echo "=== pre-commit: cargo clippy ==="
cargo clippy --quiet -- -D warnings
echo "✓ clippy clean"

echo ""
echo "=== pre-commit: cargo test ==="
cargo test --quiet
echo "✓ tests passed"

echo ""
echo "=== pre-commit: building and running all examples ==="
just examples
