Mark = Int

MonotonicClock = Unit

MonotonicClockGetResolution = Duration

MonotonicClock => MonotonicClockGetResolution {
    "get-resolution"
}

MonotonicClockNow = Mark

MonotonicClock => MonotonicClockNow {
    "now"
}
