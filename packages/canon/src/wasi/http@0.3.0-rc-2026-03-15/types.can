Headers = Handle

Headers = () => Headers

Request = Handle

Response = Handle

Response = (Body * Headers * Status) => Response

body = (Request) => Stream<String>

method = (Request) => String

path = (Request) => Option<String>

set = (Headers * String * String) => Headers
