Instant = InstantNanoseconds * InstantSeconds

InstantNanoseconds = Int

InstantSeconds = Int

SystemClock = Unit

SystemClockGetResolution = Duration

SystemClock => SystemClockGetResolution {
    "get-resolution"
}

now = (SystemClock) => Instant
