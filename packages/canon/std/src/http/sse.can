EventStream = Headers

SseEvent = String

Unit => EventStream {
    Headers().set("content-type", "text/event-stream")
}

String => SseEvent {
    "data: "
        -> Joined(String)
        -> Joined("\n\n")
}
