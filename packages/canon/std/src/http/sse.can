EventStream = Headers

SseEvent = String

() => EventStream {
    Headers().set("content-type", "text/event-stream")
}

(String) => SseEvent {
    "data: "
        .concat(String)
        .concat("\n\n")
}
