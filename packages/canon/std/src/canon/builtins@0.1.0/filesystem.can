Opened = File

Read = String

Written = Path

Path => Result<Opened, IoError> {
    "open-file"
}

File => Result<Read, IoError> {
    "read"
}

Contents * Path => Result<Written, IoError> {
    "write"
}
