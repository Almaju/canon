use Path
use canon/std/IoError

File = String

bindings "canon:builtins/filesystem@0.1.0#open-file"

File = (Path) -> Result<File, IoError>

bindings "canon:builtins/filesystem@0.1.0#read-file-handle"

read = (File) -> Result<String, IoError>
