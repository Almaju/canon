use HttpError
use InvalidUrl

Url = String

bindings "oneway:builtins/url@0.1.0#parse"

Url = (String) -> Result<Url, InvalidUrl>

bindings "oneway:builtins/http@0.1.0"

get = (Url) -> Result<String, HttpError>
