main = (Stdout) -> Result<Unit, Unit> {
    Ok(42)?.print(Stdout)
    Some(7).(
        (None) -> Unit { "absent".print(Stdout) }
        * (Some<Int>) -> Unit { "present".print(Stdout) }
    )
    Ok(Unit)
}
