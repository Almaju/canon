Birthday = String

Bit = Off + On

Bool = False + True

Byte = Bit^8

Bytes = Byte^*

Ord = Equal + Greater + Less

OtherUser = User

User = Birthday * Username

Username = String

main = (Stdout) -> Unit {
    "type definitions parsed".print(Stdout)
}
