extern Rust(".to_lowercase")
toLower = (String) -> String

extern Rust(".to_uppercase")
toUpper = (String) -> String

main = () -> Unit {
    "Hello, World"
        .toUpper()
        .print
    "GoodBye"
        .toLower()
        .print
}
