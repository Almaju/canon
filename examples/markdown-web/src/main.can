Content = Html

Init = Page

Msg = String

Page = String

Update = Page

Page => Content {
    Page -> (
        * "guide" => Content { Guide() -> Html }
        * String => Content { Intro() -> Html }
    )
}

Page => Html {
    <div class="doc">
        {Styles()}
        <nav>
            <button data-msg="Show:intro">Intro</button>
            <button data-msg="Show:guide">Guide</button>
        </nav>
        <hr>
        {Page -> Content}
    </div>
}

Unit => Init {
    Page("intro")
}

Page * Msg => Update {
    Msg -> Substring(From(1) * To(5)) -> (
        * "Show:" => Page { Msg -> Substring(From(6) * Msg -> Length -> To) -> Page }
        * String => Page { Page }
    )
}
